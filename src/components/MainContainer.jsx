import React from 'react';
import { SearchBar, UserList, UserModal } from '.';

const MainContainer = () => {
  return (
    <>
      <SearchBar />
      <UserList />
      <UserModal />
    </>
  );
};

export default MainContainer;
